ubuntu系统上使用usb_can

(1) 需要确认安装好下面的相关库：

	# sudo apt-get install libpopt-dev
	# sudo apt install make
	# sudo apt install make-guile
	# sudo apt install gcc
	# sudo apt install g++
	# sudo apt install libpopt-dev

(2) 安装pcan驱动：

	# tar xvf peak-linux-driver-8.15.2.tar.gz
	# cd peak-linux-driver-8.15.2/
	# make clean
	# sudo make netdev
	# sudo make install 
    # sudo modprobe pcan
	# ip link show | grep can
	# dmesg | grep pcan  # 应有 "pcan: driver loaded" 等信息
	# pcaninfo		（接上板子之后，输入这个命令可以查看到pcan驱动和板子的信息）
	# sudo apt update && sudo apt install -y can-utils
	# sudo ip link set can0 type can bitrate 1000000
	# sudo ip link set can0 up
	# ip link show can0  # 应该有state UP
	# ip -details link show can0 # 查看波特率

  打开新终端
    # candump can0 # 查看所有can报文




